# Ядро: формат леджера и фреймворк импортёров.
beancount>=3.2.3
beangulp==0.2.0

# Правила категоризации (rules.yaml).
PyYAML>=6

# Карточные выписки ACBA приходят в legacy-формате .xls (BIFF, не xlsx).
xlrd>=2.0

# Справка Т-Банка бывает только в PDF, и таблица в ней размечена координатами,
# а не разделителями. pdfplumber отдаёт слова вместе с координатами — по ним
# и раскладываются колонки.
pdfplumber>=0.11

# Курсы валют у ЦБ Армении (tools/fetch_rates.py). lxml и так приезжает вместе
# с pdfplumber, но здесь он нужен сам по себе — пусть будет виден явно.
requests>=2.32
lxml>=5
