# Всё, что уезжает на сервер, плюс инструменты разработки.
-r requirements-deploy.txt

# Тесты.
pytest>=8

# Бот асинхронный, и его тесты — корутины. Режим auto включён в pyproject.toml,
# поэтому помечать их не нужно.
pytest-asyncio>=0.23

# Линтер. Настройки — в pyproject.toml, там же объяснено, что отключено и зачем.
# Версия зафиксирована по минорной: pylint заводит новые проверки в минорных
# выпусках, и без этого зелёная ветка краснела бы сама по себе.
pylint>=4.0,<4.1

# Только для tools/make_acba_fixtures.py — пересборки тестовых выписок ACBA.
# В самом импорте не участвует: xlwt умеет писать .xls, xlrd только читать.
xlwt>=1.3

# Только для tools/make_tbank_fixture.py — отрисовки тестовой выписки Т-Банка.
# В самом импорте не участвует: pdfplumber умеет читать PDF, но не писать.
reportlab>=4.2
