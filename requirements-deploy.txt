-r requirements.txt

# Веб-интерфейс. Он же разбирает выписки, загруженные через браузер, — теми же
# импортёрами, что и CLI (см. fava_import_config.py).
fava>=1.30

# Бот в Telegram: приём выписок из чата тем же движком, что у CLI и fava.
# Верхняя граница по мажорной версии — между ними aiogram ломает API.
# Тянет за собой aiohttp, на котором и держится вебхук.
aiogram>=3.15,<4
