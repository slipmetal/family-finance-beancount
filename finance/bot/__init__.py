"""Бот в Telegram: принять выписку из чата и запустить импорт.

Третий интерфейс к тому же движку, что у `import.py add` и у страницы
«Выписки» в fava. Появился потому, что выписка обычно оказывается на телефоне:
банк присылает её в приложение, а оттуда она уходит в чат — и до сих пор на
этом месте требовался компьютер.

Делится работа так же, как и в двух других интерфейсах: finance/inbox.py
отвечает, кто заберёт файл и как его назвать, finance/pipeline.py держит шаги
импорта, а здесь только разговор. Ничего своего про банки и про леджер бот
не знает и знать не должен.

От страницы «Выписки» он отличается объёмом: та доводит файл до inbox и
передаёт дальше штатной вкладке Import, а бот умеет ещё и прогнать всю
цепочку — но по отдельной команде, а не сам собой. Так можно закинуть три
выписки подряд и разобрать их одним разом.
"""

from finance.bot.app import BotApp
from finance.bot.settings import Settings

__all__ = ["BotApp", "Settings"]
